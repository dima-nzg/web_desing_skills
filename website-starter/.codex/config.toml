#:schema https://developers.openai.com/codex/config-schema.json

# AGENTS.md is intentionally detailed and contains project-specific Web3 rules.
# Current file is ~45 KB UTF-8, so raise Codex project instruction budget above the 32 KiB default.
project_doc_max_bytes = 65536

[agents]
enabled = true
max_concurrent_threads_per_session = 6
