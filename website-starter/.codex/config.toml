#:schema https://developers.openai.com/codex/config-schema.json

# Portable project-scoped Codex settings for website projects.
# Keep model selection inherited from the active Codex session.

[agents]
enabled = true
max_concurrent_threads_per_session = 6
